---
  - name: "Gmaps-distance"
    signals:
      - order: "distance entre {{origin}} et {{destination}}"
    neurons:
      - say:
          message: "Calcul en cours"
      - gmaps:
          gmaps_api_key: "**********************"
          mode: "driving"
          language: "french"
          units: "metric"
          traffic_model: "pessimistic"
          args:
            - origin
            - destination
          file_template: "templates/fr_gmaps.j2"

  - name: "Gmaps-distance-from-home"
    signals: 
      - order: "distance pour aller à {{destination}}"
    neurons:
      - say:
          message: "Calcul en cours"
      - gmaps:
          gmaps_api_key: "**********************"
          mode: "driving"
          language: "french"
          units: "metric"
          origin: "22 rue archereau Paris"
          mode: "driving"
          args:
            - destination
          file_template: "templates/fr_gmaps.j2"

  - name: "Gmaps-place-address"
    signals: 
      - order: "adresse de {{search}}"
    neurons:
      - say:
          message: "Calcul en cours"
      - gmaps:
          gmaps_api_key: "**********************"
          language: "french"
          units: "metric"
          args:
            - search
          file_template: "templates/fr_gmaps.j2"
